name: jsdaddy_custom_lints
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  analyzer: ^6.4.1
  analyzer_plugin: ^0.11.3
  custom_lint: ^0.6.4
  custom_lint_builder: ^0.6.4

dev_dependencies:
  lints: ^4.0.0
